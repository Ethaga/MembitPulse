# Copy this file to .env and fill in the secrets / config values

# Membit API
MEMBIT_API_KEY=
# Optional Membit MCP URL (JSON-RPC model-context-protocol endpoint)
MEMBIT_MCP_URL=https://mcp.membit.ai/mcp

# Flowise (preferred LLM for this project)
FLOWISE_API_URL=
FLOWISE_API_KEY=

# (legacy) OpenAI (not used when Flowise-only) — keep if you want OpenAI fallback
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4

# Optional runtime overrides
PING_MESSAGE=ping
PORT=3000
NODE_ENV=production
